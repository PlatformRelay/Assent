# /owner changed (not on allow-list; ownership transfer)
decision: REVIEW
findings:
  - rule: only-safe-fields
    obligation: allowed-fields
    effect: require-review
    subject: topic-registry:orders.events.v1
    points: 0
    code: allowed-fields.sensitive
