# Gating loads policy from target ref (ADR-0015 §1). Source-branch weakening to
# challenge cannot prove this MR. Golden: MR edits its own policy → BLOCK.
# .assent/packs/ownership.yaml modified on source branch
decision: BLOCK
findings:
  - rule: meta-class
    obligation: policy-integrity
    effect: block
    subject: file:.assent/packs/ownership.yaml
    points: 0
    code: assent-policy.changed
