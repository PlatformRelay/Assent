{
  "apiVersion": "assent.dev/v1alpha1",
  "kind": "MergePolicy",
  "metadata": {"name": "topic-safety"},
  "spec": {
    "rules": [
      {
        "match": {"fileEvents": {"paths": ["topics/**"], "kinds": ["delete"]}},
        "effect": "block"
      }
    ]
  }
}
