# Decision log

Lightweight, dated operator decisions (D-nnn). ADRs cover architecture; this file covers
project/process decisions.

| ID | Date | Decision |
| --- | --- | --- |
| D-001 | 2026-07-21 | Started under working title "verdict-2". **No public GitHub repo until explicitly created by the operator.** Local git only for now. Name resolved by D-009. |
| D-002 | 2026-07-21 | License **Apache-2.0** (Kubernetes/Argo CD precedent: permissive + patent grant; supersedes the initial MIT choice same day). Fully open source; no employer names, internal system names, or internal policy content in any committed artifact. |
| D-003 | 2026-07-21 | Go module path `github.com/PlatformRelay/assent`; confirm org (PlatformRelay vs personal) before first push. |
| D-004 | 2026-07-21 | Git identity for this repo: `Konrad Heimel <konrad.heimel@gmail.com>` (local git config). No AI co-author trailers. |
| D-005 | 2026-07-21 | Development is **spec/test-driven**: openspec workflow, REQ IDs with `Test:` + `Verify:`, TDD for all implementation work. |
| D-006 | 2026-07-21 | Operator preference: **Kyverno-style declarative YAML is the primary policy surface**; Rego is an escape-hatch predicate backend inside the same envelope, never a parallel frontend (ADR-0002 v2). |
| D-007 | 2026-07-21 | **No database for now**: the per-run JSON report artifact is the storage format; `stats` aggregates report files (ADR-0009/0012). |
| D-008 | 2026-07-21 | Sample corpus: operator provides 2–3 further real self-service repo shapes (generalized, never verbatim); additionally curate **open-source repos** as public test/demo corpora (candidates in [examples/repos](../../examples/repos/README.md)). |
| D-009 | 2026-07-21 | Project name: **assent** (chosen from the [naming candidates](../planning/naming.md)). Folder, module path, CLI (`assent`), config dir (`.assent/`), and `apiVersion` group (`assent.dev/v1alpha1`) renamed accordingly. Before going public: verify GitHub name availability and the `assent.dev` domain (fallback: adjust the apiVersion group). |
| D-010 | 2026-07-21 | **Testing bar from day one**: TDD mandatory; CI-enforced **≥90% line coverage** on `internal/…` from the first implementation PR; golden decision tests + integration tests + real-forge e2e per ADR-0006. The **adopter test format is a first-class public contract** (ADR-0014) — easy policy testability for governed repos is a product feature, not tooling. |
| D-011 | 2026-07-21 | **Architecture review cycle completed**: adversarial review (15 findings, 2 critical) folded back as ADR-0015 (trust boundaries) + amendments to ADR-0003/0004/0007/0009/0011/0012; `assert` decided as hybrid CEL-leaf trees on cel-go (ADR-0013, kyverno-json dropped); engineering rules codified in [GUIDELINES.md](../../GUIDELINES.md); OSS practices adopted from sibling projects per [oss-playbook.md](../planning/oss-playbook.md). All new/amended ADRs remain **Proposed pending operator review**. |
| D-012 | 2026-07-21 | **Adoption-gated scope** (second review, part C): v1 = GitLab + `run/--dry-run/explain/test/lint` + builtin/HTTP+exec providers + `assert` backend. `rego`, gRPC, WASM, GitHub adapter, `serve`, and remote packs stay **designed seams** that unlock only when a named consumer commits — no speculative frozen contracts for tiers without users. Meta-plan gains an adoption gate: Phase 4 exits only when one real repository has run assent in anger. |
| D-013 | 2026-07-21 | Second independent review round (roast + security, in the workspace inbox) folded in: provider/plugin trust model, execution-authority matrix, resource limits, OPA capability sandbox, secret redaction, content-keyed FactQuery, finding-lifecycle state machine incl. stale-consent invalidation, `onFail` branches, per-firing points, scan confusion matrix, lint hard-errors, positions in the change model, CI security gates. Spec-hygiene drift (stale openspec/examples/C4) corrected. |
| D-014 | 2026-07-21 | **Public repo created**: [github.com/PlatformRelay/assent](https://github.com/PlatformRelay/assent) — resolves D-001/D-003 (org: PlatformRelay, matching the module path). Settings: topics, delete-branch-on-merge, wiki/projects off, dependabot alerts + security updates, secret scanning + push protection enabled. Residual from D-009: `assent.dev` domain still unverified — apiVersion group may need adjusting before contracts freeze. |
| D-015 | 2026-07-21 | **Presentation direction approved by operator**: strong defaults with tiered flexibility per ADR-0016 — config knobs / slot overrides / full templates (tiers 1–2 adoption-gated), CEL-based message interpolation (one expression language), renderer-owned envelope, PresentationModel as pinned render contract (aligned with the design roast's four-record split), render goldens + template lint, locale catalog for chrome strings. |
| D-016 | 2026-07-21 | **Third adversarial review (full design roast) adopted as ADR-0017**: merge-result pinning, named required obligations replacing anonymous vouch, `require-review` split from `challenge` (acknowledgement ≠ authorization), one-shot arming restrictions for expiring facts, `EntryRef` governed subjects + matcher domain split, typed/minimized provider protocol, `Reconcile` publication port, serialized-schemas-are-the-API (no v1 Go SDK), strict end-to-end **contract fixture as the Phase-3 gate**. Four previously-fixed risks formally refuted by the review; examples migrate to `prove`/`onFailure` when schemas land. |
| D-017 | 2026-07-21 | **First named consumer committed** (behavioral-compatibility review folded in; disposition in [named-consumer-compat.md](../planning/named-consumer-compat.md)): a private, production Kafka self-service merge gate — assent's unaffiliated **reference use case** — is the D-012 named consumer. Accepted at contract level before the Phase-3 freeze: rollout phases `off`/`observe`/`enforce` (OQ-21 lean reversed → P3-E4/ADR-0018), policy profiles + semantic comparison records + PolicyComparisonSuite promotion gates, typed ApprovalEvidence (OQ-23 → P3-E1), database-free marker/reconciliation protocol + per-MR serialization (P3-E5/ADR-0019, preserves D-007), complex-rule-backend contract unlock (E11), one sanitized compatibility fixture in the Phase-3 gate. Seam now / implement post-skeleton: merge budgets, post-merge audit (OQ-19), batch sweep (OQ-20) — E12 rescoped as the service tier. New Spike D (P2-E6) gates the optional Kubernetes CRD adapter (E14/ADR-0020). Declined/softened: full-corpus pass as a freeze precondition, forge-artifact atomic budget ledger without proof, multi-replica duplicate-prevention guarantees, command/wording parity, in-process Go rule plugins. E10/E13 stay locked. |
| D-018 | 2026-07-21 | **Operator ratifications (inbox round)**: D-017 ratified with a wording rule — the named consumer is never named and never framed as an official predecessor/successor lineage; committed docs call it the **reference use case** (assent is an independent OSS generalization of the same problem shape; D-002 sharpened). **Spike D (P2-E6) deferred to the Phase-3 window** — CR-instance validation is not needed in the first migration wave; generic value-tree gating suffices initially. **Named required obligations confirmed** (ADR-0017 §2 veto window closed — anonymous vouch stays replaced). **ADR-0009 amendment trade accepted**: forge-native all-threads-resolved gate + SHA-pinned arming; thread resolution does not re-run assent in v1; serve (E12) is the upgrade path. |
| D-019 | 2026-07-21 | **Operator ratifications (inbox round, part 2)**: ADR-0013 **CEL direction confirmed** pre-spike (final acceptance still gated on Spike A evidence in P2-E5; foreclosure warning acknowledged). **D-012 v1 scope reaffirmed** post-D-017 (GitLab-only, one-shot modes, assert backend; rego contract-unlocked/implemented post-skeleton; E12 post-Phase-4; GitHub + remote packs locked). **No AI-provenance markers** in this repo (extends D-004). **P1-E1-S01 partially unblocked**: operator provided three private self-service repo shapes as generalization sources (pointers stay workspace-local per D-002); two more to follow. |
| D-020 | 2026-07-21 | **ADR acceptance round (P2-E5 — Phase-2 gate):** ADR-0002 through ADR-0017 flipped from Proposed → **Accepted** (no full supersessions). Evidence: [adr-acceptance-review.md](../planning/adr-acceptance-review.md) + spikes [A CEL](../planning/spikes/spike-a-cel.md), [B e2e](../planning/spikes/spike-b-e2e.md), [C provider](../planning/spikes/spike-c-provider.md), [secure-setup topology](../planning/spikes/spike-secure-setup.md); forge dossiers; prior-art lessons. Partial clause supersessions noted on status lines (ADR-0017 reshapes 0003/0005/0007/0009/0010/0011/0014/0015; ADR-0016 supersedes 0012 override mechanism). OQs tagged for ADR accept closed or retagged; OQ-24 north-star timed run remains **PENDING** (topology accepted, &lt;1h unconfirmed). P2-E6 still deferred (D-018). Operator ratifies after the fact (decide-and-log). |
| D-021 | 2026-07-24 | **Roast P1-1 fix — RulesetBinding gains `require`** (schema/freeze). ADR-0017 §2 declares a binding's named required obligations; merge-policy `prove.obligation`, `evaluation-input.require`, and `lint-hard-errors.md` all referenced "a binding's require: entry", but frozen `RulesetBinding` had no such field. Added `require` per binding: optional string array, `uniqueItems` (stock-validator-visible, not `x-uniqueKeys`), absent/empty ⇒ no required obligations (vacuously covered). Closes the strongest freeze-breaker before S07 seals v1alpha1. Commit `b4c2352`. Revert: `git revert b4c2352`. |
| D-022 | 2026-07-24 | **Roast P1-2 fix — `exact` default pinned to must-contain** (adopter contract). `test-expectation.schema.json` froze `exact` with a `default: true` annotation contradicting spec REQ-P3-E1-S05-01 + prose ("must-contain by default"). Resolved to the spec's normative behaviour: `default: false`; reworded so closed-list is an opt-in; added `TestExactDefaultIsMustContain` guard. Commit `e33f3b6`. Revert: `git revert e33f3b6`. |
| D-023 | 2026-07-24 | **Named-consumer-compat portability honesty — Option B chosen** (P3-E1-S07 crux, D-017/B5). `TestExampleContractsFixturesValidate` validates fixtures with assent's own Go compiler, which asserts the vendor `x-uniqueKeys` keyword; a stock 2020-12 validator (ajv/check-jsonschema/python-jsonschema) ignores it, so "validates against the schemas" is only fully true for the assent compiler where collection uniqueness is concerned. **Chose B** (document + accept the `x-uniqueKeys` uniqueness divergence as a known, tracked limitation; prove the compat fixture's *structured fields* validate under the Go compiler and note a stock validator accepts the identical fields minus uniqueness) over **A** (add a non-Go stock validator to CI). Rationale: Go-only CI is the project norm (no ajv/check-jsonschema present; python3 lacks `jsonschema`), and `x-uniqueKeys` deliberately carries no `$vocabulary` so stock validators *ignore* it (compiler.go) — the compat fixture's six named-consumer signals (phase/profile/comparison-delta/ApprovalEvidence/marker/budget) are structured fields that do not depend on uniqueness, so their portability is **sound by construction but unproven by test** — no stock validator (ajv/python-`jsonschema`) runs in CI or in these tests, so the claim rests on the `$vocabulary` reasoning above, not on an empirical stock-validator run. Divergence is limited to collection-uniqueness on `RulesetBinding`/`MergePolicy` (both `x-uniqueKeys`-bearing), which the *strict* fixture exercises, not the compat one. Revert: drop D-023 + re-open Option A (add stock-validator CI step). |
| D-024 | 2026-07-24 | **Phase-3 exit gate (D-016 / ADR-0017 §8) STAGED — closes on merge to `main`.** Both required fixtures now exist and pass the **full local gate** (`task check`) plus the S06 sweep locally (`go test ./schemas/...`, `TestExampleContractsFixturesValidate` walks `examples/contracts/**`): the strict end-to-end **strict fixture** `examples/contracts/d016-strict-fixture/` (pinned target + merge-result digest, a renamed EntryRef whose content-derived identity `/metadata/name` stays stable across a base→head rename in a multi-entry document, exactly two independently-required obligations with two distinct proving rules, one `expired` typed fact, a missing required `ApprovalEvidence` → never `APPROVE`) and the sanitized **named-consumer compatibility fixture** `examples/contracts/named-consumer-compat/` (phase/profile/comparison-delta/ApprovalEvidence/publication-marker/budget-reservation carried as structured fields, adversarially grep-proven free of prose inference). **Schema-validation scope:** of the six named-consumer signals only `ApprovalEvidence` validates against a frozen schema today; the other five (phase/profile/comparison-delta/publication-marker/budget-reservation) are proven only to *exist as structured data* in a sweep-skipped placeholder-apiVersion doc, pending P3-E4/P3-E5 which own their final schemas. Tests: `TestD016StrictFixture`, `TestD016MissingApprovalNeverApproves`, `TestNamedConsumerCompatFixtureIsStructured`. **This entry records the milestone at HEAD of the unmerged `lane/p3-e1-s07` (stacked on the still-unmerged base `e6dbc2f`); the gate CLOSES — and Phase 5 (E1–E9) engine code may begin — only once this lane and its base land on `main` with the schemas CI job (`.github/workflows/schemas.yml`) green there.** Push/PR is blocked this session (D-001/D-009), so schemas CI has not yet run on these fixtures. The "no engine code before the gate fixture exists" rule (D-016) is thereby made auditable: fixtures staged and locally green, closure pending-merge. |
| D-025 | 2026-07-24 | **Phase-3 exit gate (D-016 / ADR-0017 §8) CLOSURE CONFIRMED.** The D-024 staged condition is met: `lane/p3-roast-p1-fixes` (P1 base `e6dbc2f`) and `lane/p3-e1-s07` (S07 exit-gate fixtures `93ea446`) landed on `main` on 2026-07-24; the **schemas CI job is green on `main` @ `93ea446`** (run 30073757754) alongside verify (30073757768), so `TestExampleContractsFixturesValidate` validated both `examples/contracts/d016-strict-fixture/` and `examples/contracts/named-consumer-compat/` in CI on `main`, not just locally. The "no engine code before the gate fixture exists" rule (D-016) is now satisfied and auditable on `main` — **Phase 5 (E1–E9) engine code is UNBLOCKED.** (P3-E5-S02 reconciliation fixtures also landed @ `2efcf21`, verify green; docs-only, path-filtered from schemas.) The D-024 schema-validation-scope caveat still stands: 5 of 6 named-consumer signals remain placeholder-shaped pending P3-E4/P3-E5. Revert: this is a factual milestone record; to reverse the gate, `git revert` the fixture commits (restores D-016's block on engine code). |
| D-026 | 2026-07-24 | **Operator bulk-ratification (via /operator-inbox).** The operator ratified, after the fact, the accumulated **decide-and-log** rounds standing in the INBOX: **D-020** ADR acceptance (ADR-0002..0017 → Accepted, accept-with-partial-supersession); **OQ-24 north-star** left PENDING (topology accepted, timed run unconfirmed) → retagged **Phase 4**; the **P2-E1 Spike A (CEL)** calls (`CrossTypeNumericComparisons(true)` + `AsCELNumber`/`AsCELBool` guards; cost budget 1000; dyn-equality silent-false hazard ⇒ require NativeTypes/typed ops for obligation proof); **P2-E2 Spike B (e2e)** calls (testcontainer as CI default, kind for local/demo; kind side-load via `docker save --platform` + `kind load image-archive`; SHA-guard steady-state = HTTP **409**); **P2-E3 Spike C (provider)** calls (versioned envelopes with declaration echo; env scrubber refuses configured `*TOKEN*`/`*SECRET*` names; per-fact-type `maxAge` tiers); **P2-E4 secure-setup** topology (Premium + external-CI-project + project-access-token + all-threads-resolved + merged-results + merge trains + approval rules + `.assent/**` human gate as the single supported v1 topology); **P1-E2** archetypes (holdout adjudication = operator task; `/owner` escalation to require-review; delete `block`/rename `require-review`); **P1-E1** corpus (kubernetes/org + JulieOps + octoDNS; S01 samples from vision; sanitization semantics); **P1-E3** forge dossier (live `merge?sha=` verification deferred to P2-E2 smoke); **P1-E4** prior-art judgment calls; and the already-landed **D-021/D-022** (schema-freeze one-line fixes @ `e6dbc2f`) and **D-023/D-024/D-025** (P3-E1-S07 §8 exit-gate + named-consumer fixtures + D-016 closure). **Residual preserved — NOT closed by this ratification:** the roast's **P1-3** finding stands open — `x-uniqueKeys` is a **Go-only keyword invisible to stock JSON-schema validators** (ajv / check-jsonschema / python-`jsonschema`), so "serialized schemas are the API" holds for structure but not for collection-uniqueness under a stock validator. D-023 chose to *accept and track* this divergence (Option B), which is a disposition, **not** a fix; ratifying the freeze must not paper over it. P1-3 remains a **residual to resolve before the frozen contract is relied on externally** (escape hatch = D-023 Option A: add a stock-validator CI step). Revert: this is a factual ratification record; drop D-026 to reopen the listed rounds as awaiting-approval. |
| D-027 | 2026-07-25 | **P1-3 residual → Option A (stock-validator CI).** Operator chose **A** — add a non-Go Draft 2020-12 stock validator to CI — superseding D-023's Option B disposition for the open residual (D-023's freeze-time honesty note stands as history; the tracked limitation is no longer "accept forever"). **Scope of A:** (1) schemas CI runs a stock validator (prefer `check-jsonschema` or `ajv`) over frozen `schemas/**` + `examples/contracts/**` so structure/`$ref` bundling is proven outside the Go compiler; (2) uniqueness that only `x-uniqueKeys` can express stays Go-enforced until a follow-up migrates named collections to stock-visible constraints (`uniqueItems` / reshaped flats) where feasible — A does **not** pretend stock tools suddenly understand the vendor keyword. Backlog: open hygiene story under Phase-3 freeze residuals / pre–external-reliance (see `openspec/specs/backlog.md`). Revert: restore D-023-B as the standing residual and drop the stock-validator CI story. |
| D-028 | 2026-07-25 | **OQ-1 domain — `assent.dev` unavailable.** Operator reports the domain is already owned by a third party. Activates the D-009 / prior freeze-B rename path: **do not keep `assent.dev` as the public apiVersion / `$id` group** (would point `$id` URLs and adopter trust at someone else's DNS). **Group replacement is TBD** — operator must pick before any "contracts frozen / public freeze" marketing and ideally before Phase-4 adopter-facing samples harden further. Candidates and recommendation live in INBOX + OQ-1; once chosen, one coordinated rename across `apiVersion` consts, schema `$id`s, docs, and fixtures (mechanical; high churn, low design risk). OQ-24 north-star timed run remains an open Phase-4 backlog operator task (unchanged by this entry). Revert: N/A (factual); picking a group closes the TBD. |
| D-029 | 2026-07-25 | **D-008 extra private shapes — defer, keep in corpus.** Operator: defer delivering shapes 4–5 now; **do not drop** them from the corpus plan. Standing three generalized samples + OSS corpus (OQ-16) remain the committed set; additional private-shape generalization stays an optional corpus expansion tracked on P1-E1 / `examples/repos/` (D-019 "two more to follow" → deferred, not cancelled). Does not block Phase 4. Revert: re-open shapes 4–5 as near-term operator tasks. |
| D-030 | 2026-07-25 | **Phase-3 freeze — Accept ADR-0018 + ADR-0019.** Operator accepted both (was Proposed). Index rows + Status lines flipped to **Accepted**. Closes the P3-ADR-freeze backlog residual; Phase-4 impl may cite these ADRs as accepted contract. Revert: flip Status back to Proposed (does not un-land schemas). |
| D-031 | 2026-07-25 | **OQ-1 replacement path = Option A (own a domain).** Operator ratified **A** from the D-028 options: register a domain under PlatformRelay control and use it as apiVersion/`$id` group (e.g. candidates `assent.run`, `assent.tools`, … — verify at registrar). **Exact domain string still required** before the rename lane runs. B (subdomain of existing PlatformRelay domain) and C (GitHub Pages-only) remain available if A purchase fails. Revert: reopen A/B/C pick. |
| D-032 | 2026-07-25 | **HostMajor negotiation = Option A (shipped).** Operator ratified parse-major-from-`apiVersion` (`provider.assent.dev/v1alpha1` → `1`, `HostMajor = 1`; missing/unparseable → mismatch fail-closed). Rejects B (explicit `protocolMajor` schema field) and C (whole version string as key). Lane already on main; no code change. Revert: open a schema story for B and revert the provider major-parse tests/impl. |
| D-033 | 2026-07-26 | **EvaluationInput does not carry pinned SHAs; they live in DecisionRecord.pins.** The frozen `evaluation-input.schema.json` is `additionalProperties: false` with no SHA field; P4-E1-S01 relocates pinned source/target/merge-result SHAs out-of-band, captured once at the CI-env edge and emitted into `DecisionRecord.pins` (which carries `sourceSha`/`targetSha`/`mergeResultDigest`). Chosen over editing the Phase-3-frozen schema (D-016) or an additive extension. Revert: unfreeze the schema and add SHA fields. |
| D-034 | 2026-07-26 | **P4-E1-S05 doctor protected-source signal is a documented INSECURE PLACEHOLDER; real verification deferred to E4/E5.** The pure default-deny arming logic is correct, but the env reader (`ASSENT_PIPELINE_CONFIG_PROTECTED`) is spoofable by an author-editable CI (the exact ADR-0015 §4 self-assertion). Live GitLab forge-capability probing is out of P4-E1 scope. HARD GATE: real protected-source verification MUST replace the placeholder before any approve/merge write (S06/S08) is gated on `ArmEligible`. Revert/close: implement the real check in E4/E5 and delete the placeholder. |
| D-035 | 2026-07-26 | **P4-E1 S10 (L3 live-GitLab e2e) + S11 (D-012 real-repo adoption) parked for the operator.** Infra-gated: S10 needs a live/containerized GitLab + token + testcontainer runtime; S11 needs one REAL repo on live MRs (D-012 says synthetic does not count). The autonomous loop builds S01–S09 + S12 only; these two are the operator-run Phase-4 exit-gate finishers. Revert: operator provides infra and asks the loop to attempt S10. |
| D-036 | 2026-07-26 | **Red-main gitleaks false positive fixed via comment reword + narrow allowlist fingerprint.** main went red at `99be3c0` when gitleaks flagged a DOC COMMENT (a slash-delimited word run) in `internal/change/diff.go`. Because CI scans full history (`fetch-depth: 0` + `--log-opts=HEAD`) and history-rewrite is forbidden, the fix is a `.gitleaksignore` allowlisting the single historic S02-commit fingerprint (one finding; does not weaken gitleaks globally) plus a comment reword for current-source hygiene. Harness note: `task check` does not run gitleaks/govulncheck (verify.yaml does) — run gitleaks `--log-opts=HEAD` locally before landing. |
| D-037 | 2026-07-26 | **Live forge lab + credential location for P4-E1-S10/S11.** Operator designated a private **gitlab.com** lab project (`assent-lab`) as the D-012 / S11 real-repo target. Auth for agents: workspace direnv env var **`GITLAB_TOKEN`** (host `gitlab.com`) — **token value never committed**; concrete path/IDs live only in gitignored `agent-context/LOCAL-INFRA.md`. Does **not** by itself close S10 (still needs Spike-B testcontainer/kind runtime) or S11 (still needs an explicit operator ask to run assent on live MRs after S09). Revert: rotate/revoke the token in GitLab and update/remove LOCAL-INFRA. |
| D-038 | 2026-07-26 | **Local kind GitLab lab is authorized but deferred.** Operator: do **not** land the long-lived `hack/kind` setup/status/teardown + `task kind-up` lab now; **do** record that it **should** be done and **is allowed** (Spike B / OQ-6 already picked testcontainer for CI and kind for local/demo). An early implementation was reverted the same day. When a lane picks this up: promote `hack/spikes/e2e/boot-kind.sh` into an idempotent lab under `hack/kind/` (reuse Omnibus slim config + `kind load image-archive`), wire Task targets, keep CI on testcontainer. Until then the scaffold (`kind-config.yaml` + README) and spike harness remain. Revert: cancel the authorization or implement immediately via a dedicated lane. |
| D-039 | 2026-07-26 | **P4-E1 autonomous walking-skeleton slices complete on `main` (through `aaeff79`).** Landed via TDD → gate → fresh independent review → ff-merge: S03 obligations aggregation; S04 DecisionRecord+PresentationModel report; S06+S08 Reconcile (post one resolvable thread / approve + SHA-pinned merge against the in-memory fake); S07 trust-boundary goldens (`.assent/**`→assent-policy BLOCK, SHA-guard rejection, tokenless evaluation path); S09 e2e harness wiring (compiles/vets under `-tags e2e`, skips without infra); S12 determinism double-run + rerun/crash-then-rerun idempotence + deterministic lowest-numeric-forge-ID duplicate-repair with additive `PublicationReceipt.repairs`. Two engine lanes needed 2 review rounds each: S03 (a real multi-entry non-numeric fail-open reaching APPROVE on an unproven obligation — closed by omitting unbound `old`/`new` from the CEL activation so any reference errors to REVIEW) and the forge lane (an untested atomic MergeCAS SHA-guard branch masked by the pre-check — closed by a TOCTOU racing-fake test). **D-034 UNCHANGED / not tripped:** S08 gates writes on injected `ArmEligible` test data against the fake, never the INSECURE-PLACEHOLDER env reader; real protected-source verification before a real approve/merge write remains the open E4/E5 hard gate. **S12 F1 deferred to E4/E12:** an all-unparseable-forge-ID canonical tie is scan-order-dependent but unreachable (real note IDs are numeric); optional one-line secondary-string-tiebreak hardening. Remaining Phase-4 exit-gate = S10+S11 (operator-run, D-035). Revert: `git revert` the slice commits `76d70c7 ae60727 0f0c726 4429581 57179f7 aaeff79`. |
| D-040 | 2026-07-26 | **Provider contract-test `-race` exec deadline 1s→5s — no data race exists.** Two independent investigations found NO data race in `hack/spikes/provider`; the intermittent `signal: killed` in `TestContract` was its 1s `CallExec`/`CallHTTP` deadline exceeded under `-race` instrumentation (the parent stdin-copy goroutine is CPU-starved, so the `toyexec` child blocks past 1s). Fixed by matching the already-stable `isolation_test.go` 5s deadline — a correct under-provisioned-harness-deadline fix, NOT race-masking (there is no race to mask). Commit `a7b3579`. Revert: `git revert a7b3579`. |
| D-041 | 2026-07-27 | **P4-E1-S10 walking skeleton GREEN on live gitlab.com.** The real GitLab REST v4 forge adapter (`internal/forge/gitlab`) + `assent run` orchestration landed on `main` @ `4addc3d` (independently reviewed APPROVE; all four attacks — unintended-write, SHA-guard/synthetic-digest honesty, policy-from-target-ref, token redaction — actively driven and held). Proven end-to-end against the live lab `konrad.heimel/assent-lab`: (M1) MR!1 `partitions 6→3` → REVIEW → one resolvable thread with the ADR-0019 marker; (M2) two reruns → ZERO new threads (live rerun-idempotence, REQ-S10-02 analogue); (M3) MR!2 `partitions 6→12` with `--arm` → APPROVE → approve + SHA-pinned merge (merged `960c6dd`, pinned to evaluated source `9a816d4`); unarmed APPROVE → advisory-only, zero writes. Each run emitted a schema-valid DecisionRecord with honest `mergeResultDigest: null` + `capabilityGap` (gitlab plain-merge has no merge-result digest; the CAS uses a synthetic source+target digest that never leaks into the record). `--arm` is a SANDBOX override — it does NOT satisfy the D-034 real-protected-source verification (S05 INSECURE-PLACEHOLDER still deferred); `readPipelineDescription` is never wired into the merge path. Evidence: `docs/decisions/evidence/p4-e1-s11-adoption/`. Revert: `git revert 4addc3d`. |
| D-042 | 2026-07-27 | **D-012 adoption gate SATISFIED / P4-E1-S11 done — Phase-4 exit gate CLOSED.** A real gitlab.com repository (`konrad.heimel/assent-lab`, the D-037-designated lab — a real GitLab project with real API/merge semantics, not a synthetic testcontainer fixture) ran assent on LIVE merge requests: MR!1 (REVIEW, resolvable thread) and MR!2 (APPROVE, real SHA-pinned merge `960c6dd`). DecisionRecords retained under `docs/decisions/evidence/p4-e1-s11-adoption/`; MRs: https://gitlab.com/konrad.heimel/assent-lab/-/merge_requests/1 and /2. With the S01–S12 autonomous slices (D-039) + S10 live green (D-041) + this adoption record, **the P4-E1 walking-skeleton exit gate is closed; Phase 5 (E1–E9) is unblocked.** FOLLOW-UP (S10 review F1, P2): `assent run` diffs only the single governed subject, so the `.assent/**`→`assent-policy` BLOCK routing is reachable only at the S07 engine tier, not yet on the live adapter path — the full-engine phase (E1) should enumerate the MR's changed-file set so the self-vouch BLOCK is exercised live (today the live self-vouch protection is policy-from-target-ref, which holds and was verified). Revert: factual milestone; to reopen, revert the S10 lane. |
