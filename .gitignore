# Binaries & build output
bin/
dist/
*.test
coverage*.out
cov.out

# Go
vendor/

# Node (hack/schemas-validator: only package.json + package-lock.json are committed)
node_modules/

# Docs site
site/
.venv*/
.task/

# Editors / OS
.DS_Store
.idea/
*.swp

# Local env & secrets
.envrc
.env
*.local

# Agent workspace (session-local, never committed)
agent-context/
.claude/*
!.claude/settings.json
/assent

# Session worktrees (agent-loop-local lanes)
.worktrees/

# Reference repos (third-party/internal IP — read-only inputs, NEVER committed;
# only generalized, generated equivalents may enter the tree — D-002 / AGENTS.md rule 1)
references/
